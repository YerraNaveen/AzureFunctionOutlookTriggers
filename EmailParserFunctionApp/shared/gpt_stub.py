"""Stub implementations for GPT-based checks."""

def gpt_html_filter_check(html: str) -> bool:
    """Pretend to validate HTML content."""
    return True


def gpt_relevance_check(text: str) -> bool:
    """Pretend to check SLT relevance."""
    return True
